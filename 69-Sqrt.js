// Define a function named mySqrt that takes a number x
var mySqrt = function (x) {
  // Step 1: Find the square root of x using Math.sqrt
  // Step 2: Use Math.floor to round it down to the nearest integer
  // Step 3: Return the floored value
  return Math.floor(Math.sqrt(x));
};
